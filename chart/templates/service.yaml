apiVersion: v1
kind: Service
metadata:
  name: {{ include "blueprince-helper.fullname" . }}
  labels:
    {{- include "blueprince-helper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blueprince-helper.selectorLabels" . | nindent 4 }}
